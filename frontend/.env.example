# StockTrader — Frontend Environment Variables
# Copy this file to `.env` only if you need to override defaults.

# Only required for production builds. In dev, the Vite proxy (vite.config.js)
# forwards /api to http://localhost:3000 automatically.
VITE_API_URL="http://localhost:3000"
